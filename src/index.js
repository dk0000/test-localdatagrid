import React from "react";
import ReactDOM from "react-dom";

import LocalDataGrid from "./LocalDataGrid";

const rootElement = document.getElementById("root");
ReactDOM.render(<LocalDataGrid />, rootElement);
